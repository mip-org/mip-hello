name: Delete Packages

on:
  workflow_dispatch:
    inputs:
      pattern:
        description: 'Filename prefix to match (e.g. "finufft-2.5.0-any" or "finufft-" for all finufft)'
        required: true
        type: string
      dry_run:
        description: 'Dry run (show what would be deleted without deleting)'
        required: true
        type: boolean
        default: true
      rebuild_index:
        description: 'Rebuild and deploy the package index after deletion'
        required: true
        type: boolean
        default: true

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  delete:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pyyaml

      - name: List matching packages
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python scripts/delete_packages.py --pattern "${{ inputs.pattern }}" --dry-run

      - name: Delete packages
        if: ${{ !inputs.dry_run }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python scripts/delete_packages.py --pattern "${{ inputs.pattern }}"

  rebuild-index:
    runs-on: ubuntu-latest
    needs: delete
    if: ${{ !inputs.dry_run && inputs.rebuild_index }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pyyaml

      - name: Assemble package index
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python scripts/assemble_index.py

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/gh-pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: rebuild-index
    if: ${{ !inputs.dry_run && inputs.rebuild_index }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
